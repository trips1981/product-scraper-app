# Copy this to .env and fill in your values before running docker-run.bat
# (Never commit .env to git)

OPENAI_API_KEY=sk-...

# Optional overrides (defaults work fine in Docker)
# BROWSER_CHANNEL=chromium
# HEADLESS=true
